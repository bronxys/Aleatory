const fs = require('fs'); const https = require('https'); const { execSync } = require('child_process'); https.get('https://mathiasbynens.be/demo/animated-webp-supported.webp', (res) => { let chunks = []; res.on('data', c => chunks.push(c)); res.on('end', () => { let buffer = Buffer.concat(chunks); fs.writeFileSync('test.webp', buffer); try { execSync('ffmpeg -y -i test.webp -movflags faststart -pix_fmt yuv420p -vf "scale=trunc(iw/2)*2:trunc(ih/2)*2" test.mp4', { stdio: 'pipe' }); console.log('ffmpeg convert success'); } catch (e) { console.error('ffmpeg failed:', e.stderr ? e.stderr.toString() : e.message); } }); });
